name: Hello World
on: [push]
jobs:
    hello-world-job:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Hello World
              run: python hello.py